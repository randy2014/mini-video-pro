# 服务器迁移手顺书

> 迁移日期：2026-08-04  
> 源服务器：`43.161.222.78` (Ubuntu 24.04)  
> 目标服务器：`64.90.19.6` (Ubuntu 24.04)

---

## ⚠️ 迁移前确认

- [ ] 域名 DNS 已准备好切换（如涉及域名）
- [ ] 新服务器已安装 Docker + Docker Compose
- [ ] 新服务器防火墙/安全组已放行以下端口：
  - `80, 443`（如有 Nginx）
  - `8081`（video-backend）
  - `8082`（video-frontend / APK 下载）
  - `3307`（MySQL，建议仅内网）
  - `6380`（Redis，建议仅内网）
  - `21115-21119`（RustDesk）

---

## 一、备份源服务器数据

### 1.1 备份 MySQL 数据库

```bash
# 在源服务器执行
docker exec video-mysql mysqldump -uroot -pVideoPro@2024! \
  --single-transaction --routines --triggers --add-drop-database \
  --databases video_entitlement \
  > /data/backup/video_entitlement_$(date +%Y%m%d_%H%M%S).sql

# 确认备份文件大小
ls -lh /data/backup/
```

### 1.2 备份 /data 数据目录

```bash
# 在源服务器执行
tar -czf /data/backup/video-data-$(date +%Y%m%d).tar.gz /data/video-apk /data/rustdesk
```

### 1.3 备份 Docker Volume 数据（可选兜底）

```bash
# MySQL volume
docker run --rm -v video-mysql-data:/data -v /data/backup:/backup alpine \
  tar -czf /backup/mysql-volume-$(date +%Y%m%d).tar.gz -C /data .

# Redis volume  
docker run --rm -v video-redis-data:/data -v /data/backup:/backup alpine \
  tar -czf /backup/redis-volume-$(date +%Y%m%d).tar.gz -C /data .
```

---

## 二、迁移数据到新服务器

### 2.1 传输备份文件

```bash
# 从源服务器 scp 到新服务器（在新服务器执行）
mkdir -p /data/backup
scp root@43.161.222.78:/data/backup/*.sql /data/backup/
scp root@43.161.222.78:/data/backup/video-data-*.tar.gz /data/backup/
```

### 2.2 恢复数据目录

```bash
# 新服务器执行
tar -xzf /data/backup/video-data-*.tar.gz -C /
# 确认：ls -la /data/video-apk /data/rustdesk
```

### 2.3 创建 Docker 网络

```bash
docker network create video-net
```

### 2.4 启动 MySQL

```bash
docker run -d --name video-mysql --restart always \
  --network video-net \
  -p 3307:3306 \
  -e MYSQL_ROOT_PASSWORD=VideoPro@2024! \
  -v /var/lib/docker/volumes/video-mysql-data/_data:/var/lib/mysql \
  mysql:8.0
```

### 2.5 导入数据库 & 创建应用用户

```bash
# 等待 MySQL 启动完成（约 30 秒）
sleep 30

# 导入数据
docker exec -i video-mysql mysql -uroot -pVideoPro@2024! < /data/backup/video_entitlement_*.sql

# ⚠️ mysqldump 默认不导出非 root 用户，需手动创建 video_user
docker exec video-mysql mysql -uroot -pVideoPro@2024! -e "
  CREATE USER IF NOT EXISTS 'video_user'@'%' IDENTIFIED BY 'VideoUser@2024!';
  GRANT ALL PRIVILEGES ON video_entitlement.* TO 'video_user'@'%';
  FLUSH PRIVILEGES;
"

# 验证：root + video_user 均能连接
docker exec video-mysql mysql -uroot -pVideoPro@2024! -e "SELECT 1" && echo "root ✅"
docker exec video-mysql mysql -uvideo_user -pVideoUser@2024! -e "SELECT 1" && echo "video_user ✅"

# 确认表数量
docker exec video-mysql mysql -uroot -pVideoPro@2024! -e \
  "SELECT COUNT(*) AS tables FROM information_schema.TABLES WHERE TABLE_SCHEMA='video_entitlement';"
# 应输出：20（含 flyway_schema_history 则是 21）
```

### 2.6 启动 Redis

```bash
docker run -d --name video-redis --restart always \
  --network video-net \
  -p 6380:6379 \
  -v /var/lib/docker/volumes/video-redis-data/_data:/data \
  redis:7-alpine redis-server --requirepass VideoUser@2024!
```

### 2.7 启动后端

```bash
# 从 GHCR 拉取最新镜像
docker pull ghcr.io/randy2014/mini-video-pro-backend:latest

docker run -d --name video-backend --restart always \
  --network video-net \
  -p 8081:8080 \
  -e DB_HOST=video-mysql \
  -e DB_USERNAME=video_user \
  -e DB_PASSWORD=VideoUser@2024! \
  -e REDIS_HOST=video-redis \
  -e REDIS_PASSWORD=VideoUser@2024! \
  -e JWT_ACCESS_SECRET=<GitHub Secrets: JWT_ACCESS_SECRET> \
  -e JWT_REFRESH_SECRET=<GitHub Secrets: JWT_REFRESH_SECRET> \
  -e ENTITLEMENT_HMAC_KEY=<GitHub Secrets: ENTITLEMENT_HMAC_KEY> \
  -e SWAGGER_ENABLED=false \
  -e APP_APK_DIR=/data/video-apk \
  -e APP_DOWNLOAD_BASE=http://64.90.19.6:8082/downloads \
  -v /data/video-apk:/data/video-apk \
  ghcr.io/randy2014/mini-video-pro-backend:latest
```

> **✅ 已验证的值**（源服务器容器 env）：
> - `DB_PASSWORD=VideoUser@2024!`（与 Redis 密码相同）
> - `REDIS_PASSWORD=VideoUser@2024!`
> - `DB_USERNAME=video_user`、`DB_HOST=video-mysql`、`REDIS_HOST=video-redis`
> 
> **🔐 需从 GitHub Actions Secrets 获取**（无法从容器 env 反查）：
> - `JWT_ACCESS_SECRET`、`JWT_REFRESH_SECRET`、`ENTITLEMENT_HMAC_KEY`
> - 登录 GitHub → `randy2014/mini-video-pro` → Settings → Secrets and variables → Actions 查看

### 2.8 启动前端

```bash
docker pull ghcr.io/randy2014/mini-video-pro-frontend:latest

docker run -d --name video-frontend --restart always \
  --network video-net \
  -p 8082:80 \
  -v /data/video-apk:/usr/share/nginx/html/downloads:ro \
  ghcr.io/randy2014/mini-video-pro-frontend:latest
```

### 2.9 启动 RustDesk

```bash
# 先启动 hbbs 生成密钥（第一次会生成 /data/rustdesk/id_ed25519*）
docker run -d --name rustdesk-hbbs --restart always \
  -p 21115:21115 -p 21116:21116 -p 21116:21116/udp -p 21118:21118 \
  -v /data/rustdesk:/root \
  rustdesk/rustdesk-server hbbs

docker run -d --name rustdesk-hbbr --restart always \
  -p 21117:21117 -p 21119:21119 \
  -v /data/rustdesk:/root \
  rustdesk/rustdesk-server hbbr
```

---

## 三、更新 CI/CD

### 3.1 更新 GitHub Actions Secrets

登录 GitHub → `randy2014/mini-video-pro` → Settings → Secrets and variables → Actions：

| Secret 名称 | 新值 |
|-------------|------|
| `SERVER_HOST` | `64.90.19.6` |
| `SERVER_SSH_KEY` | `<新服务器 SSH 私钥>` |

> ⚠️ `DB_PASSWORD` / `JWT_*` / `ENTITLEMENT_HMAC_KEY` 等值**保持不变**（数据库内容相同）。

### 3.2 更新 deploy.yml（如 server host 用变量）

`deploy.yml` 中 `SERVER_HOST` 从 `${{ secrets.SERVER_HOST }}` 取值，更新 secret 即可，无需改文件。

### 3.3 配置新服务器 SSH 访问

```bash
# 在新服务器上
# 把 GitHub Actions 用的公钥加到 authorized_keys
mkdir -p ~/.ssh
echo "<公钥内容>" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---

## 四、验证清单

| 检查项 | 命令 | 预期 |
|--------|------|------|
| MySQL 连接 | `docker exec video-mysql mysql -uroot -pVideoPro@2024! -e "SELECT 1"` | `1` |
| Redis 连接 | `docker exec video-redis redis-cli -a VideoUser@2024! PING` | `PONG` |
| 后端启动 | `docker logs video-backend --tail 5` | 含 `Started VideoEntitlementApplication` |
| 前端可访问 | `curl -sI http://64.90.19.6:8082/` | `HTTP/1.1 200` |
| 后端 API | `curl -s http://64.90.19.6:8081/admin/api/v1/auth/login -X POST -H "Content-Type: application/json" -d '{"username":"admin","password":"Admin@123"}'` | 返回 token |
| APK 下载 | `curl -sI http://64.90.19.6:8082/downloads/video-entitlement-latest.apk` | `200` |
| CI 部署 | push 到 main → 查看 GitHub Actions 是否成功 | `success` |
| RustDesk 连接 | 客户端填新 IP + Key，测试远程连接 | 可连接 |

---

## 五、切流 & 回滚

### 5.1 切流

1. DNS 或客户端配置指向新 IP
2. 等待 DNS 生效（或手动测试新 IP 功能正常）
3. 确认旧服务器上的用户流量已降至 0

### 5.2 回滚（如果需要）

1. DNS 切回旧 IP
2. 在旧服务器上 `docker start video-backend video-frontend video-mysql video-redis`
3. 排查新服务器问题后重新迁移

---

## 六、注意事项

1. **不要动 `mini-novel-*` 容器**：这些属于其他项目（原则 #2），迁移时不要操作它们
2. **系统 Nginx**（`xs2026.site`）：当前代理的是 `mini-novel` 项目，与本项目 `video-*` 无关；本项目服务直接暴露出 `8081`/`8082` 端口
3. **已验证的密码**：MySQL root=`VideoPro@2024!`，应用用户=`video_user`/`VideoUser@2024!`，Redis=`VideoUser@2024!`，Admin 后台=`admin`/`Admin@123`
4. **`video_user` 数据库用户**：已在「2.5 导入数据库」步骤中创建，无需额外操作
5. **RustDesk Key**：恢复 `/data/rustdesk/id_ed25519.pub` 后，客户端 Key 不变，无需重新配置
6. **APP_DOWNLOAD_BASE**：目标服务器 IP 已确定为 `64.90.19.6`，启动后端时 `APP_DOWNLOAD_BASE=http://64.90.19.6:8082/downloads`

---

## 七、获取 GitHub Token（推送代码 / 触发 CI 用）

### 方式一：经典 token（repo 整项权限，简单）

1. 打开 `https://github.com/settings/tokens/new`
2. **Note** 填 `mini-video-pro-push`，**Expiration** 选 7 days
3. 勾选 **repo**（整项，含仓库读写）
4. 点最底部 **Generate token**
5. 复制 token（以 `ghp_` 开头，**仅显示一次**）

### 方式二：细粒度 token（权限最小化，推荐）

1. 打开 `https://github.com/settings/personal-access-tokens/new`
2. **Token name** 填 `mini-video-pro-push`，**Expiration** 选 7 days
3. **Repository access** 选 `Only select repositories` → 搜索勾选 `randy2014/mini-video-pro`
4. **Repository permissions** → `Contents` 设为 `Read and write`，其余保持 `No access`
5. 点 **Generate token**，复制（以 `github_pat_` 开头）

> ⚠️ 原则：token 只用一次、用完即吊销，**不写入任何文件/git 配置**。
> 推送示例（Windows 沙箱需 `http.sslBackend=openssl` 绕过 schannel）：
>
> ```bash
> git -c http.sslBackend=openssl push https://randy2014:<TOKEN>@github.com/randy2014/mini-video-pro.git main
> ```

---

## 八、Android APK 打包与版本登记

> ⚠️ **重要**：`build-android.yml` 只负责「构建 + 上传 APK 文件到 `/data/video-apk`」，**不会自动登记到 APP 版本管理表**。登记是独立步骤，**每次重新打包后都必须手动做**，否则后台「APP 版本管理」里看不到新包。

### 8.1 触发打包

- GitHub Actions → `Build Android APK` → **Run workflow**（`workflow_dispatch` 手动触发，push 不会自动触发）
- 或 API：`POST /repos/randy2014/mini-video-pro/actions/workflows/build-android.yml/dispatches`

产物：签名 release APK + 二维码，scp 上传到 `/data/video-apk`，文件名 `video-entitlement-v{versionName}-{YYYYMMDD}-{HHmmss}.apk`。

### 8.2 登记版本表（二选一）

**方式 A：后台 UI**
登录 `http://64.90.19.6:8082` → APP 版本管理 → 发布新版本 → 填 versionName / versionCode / 下载地址（`http://64.90.19.6:8082/downloads/` + 文件名）。

**方式 B：接口登记**
```bash
# 1) 拿 admin token
TOKEN=$(curl -s -X POST "http://64.90.19.6:8082/admin/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['adminToken'])")

# 2) 登记新版本
curl -s -X POST "http://64.90.19.6:8082/admin/api/v1/versions" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"versionName":"0.9.202609010208","versionCode":9010208,"downloadUrl":"http://64.90.19.6:8082/downloads/video-entitlement-v0.9.202609010208-20260901-020938.apk","releaseNotes":"更新说明","forceUpdate":false}'

# 3) 停用旧版本（可选，保持只有最新版 ACTIVE）
curl -s -X PUT "http://64.90.19.6:8082/admin/api/v1/versions/{旧版本id}/status?status=INACTIVE" \
  -H "Authorization: Bearer $TOKEN"
```

### 8.3 versionCode 推导规则

`build.gradle.kts` 里：`versionName = 0.9.{yyyyMMddHHmm}`，`versionCode = {MMddHHmm}`（即 versionName 时间戳的**后 8 位**）。

例：versionName `0.9.202609010208` → versionCode `9010208`。
