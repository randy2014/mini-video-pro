# 2026-09-15 域名接入变更：http://64.90.19.6:8082 → https://xs2026.site/

> 需求：把「访问域名」从 `http://64.90.19.6:8082/platform` 更新为 `https://xs2026.site/`，
> 并重新打包 APK 并上传。

---

## 一、结论：域名不在本项目手里（本次最大发现）

| 项 | 事实 |
|----|------|
| xs2026.site 的 80/443 | 由**另一个项目 mini-novel** 的网关容器 `mini-novel-gateway`（nginx 1.27.5）持有 |
| 网关配置 | `/opt/mini-h5/deploy/gateway/nginx.conf` |
| 该网关原本只服务 | mini-novel：`/`(H5)、`/admin/`、`/admin-api/`、`/crawler-api/` |
| 本项目原本 | 只按 IP+端口暴露：`video-frontend`:8082、`video-backend`:8081，**未接入域名** |
| 证书 | 仅覆盖 `xs2026.site` + `www.xs2026.site`（无泛域名，有效期至 2026-12-14） |

⚠️ 这与 `迁移手顺.md` 第六节第 2 条记载一致（"系统 Nginx（xs2026.site）当前代理的是 mini-novel
项目，与本项目 video-* 无关"），也与项目原则 #2「不碰其他项目的 Nginx」**直接冲突**。
本次经用户明确授权，采用**纯新增**方式接入（不改 mini-novel 任何现有路由）。

---

## 二、最终架构（子路径方案）

```
                     ┌─────────────────────────────────────────────┐
  443  https://xs2026.site  ──►  mini-novel-gateway (nginx)        │
                     │                                             │
                     ├─ /            ──► mini-novel-h5      （原有，未动）
                     ├─ /admin/      ──► mini-novel-admin-ui（原有，未动）
                     ├─ /admin-api/  ──► mini-novel-admin-ui（原有，未动）
                     ├─ /crawler-api/──► crawler-service    （原有，未动）
                     │                                             │
                     ├─ /platform/   ──► video-frontend:80   （新增）
                     ├─ /downloads/  ──► video-frontend:80   （新增）
                     ├─ /admin/api/  ──► video-backend:8080  （新增）
                     └─ /api/v1/     ──► video-backend:8080  （新增）
                     └─────────────────────────────────────────────┘
```

**无冲突依据**：mini-novel 只用 `/`、`/assets/`、`/api/`、`/cover/`、`/admin/`、`/admin-api/`、
`/crawler-api/`；本项目用的是 `/platform/`、`/downloads/`、`/admin/api/`、`/api/v1/`。
（实测确认 `/opt/mini-h5` 全目录 grep `api/v1` **零命中**。）

### 关键设计点

1. **前缀在哪一层剥？** 网关**原样转发** `/platform/...`（不剥前缀），由本项目
   `video-entitlement-admin/docker/nginx.conf` 内部 `rewrite ^/platform/(.*)$ /$1 last;` 剥掉。
   好处：`http://64.90.19.6:8082/` 与 `https://xs2026.site/platform/` **两条入口同时可用**，
   IP 直连始终是可用的兜底。
2. **前端必须改 base**：Vite 默认 `base: '/'` 会让资源走 `/assets/...`，而 mini-novel H5
   **也占用 `/assets/`** → 直接挂子路径会互相覆盖。故 `vite.config.ts` 设 `base: '/platform/'`，
   `main.tsx` 的 `BrowserRouter` 加 `basename="/platform"`。
3. **网络打通**：`video-*` 在 `video-net`，mini-novel 在 `deploy_default`，网关只能解析同网络容器。
   用 `docker network connect deploy_default video-frontend video-backend`（**纯新增连接**，
   不改动 mini-novel 任何容器/网络/配置）。容器每次被 CI 重建都会掉网，故 `deploy.yml` 已加这一步。

---

## 三、踩坑记录（重点）

### ⚠️ 坑 1：网关单文件 bind mount 会缓存 inode，改配置 reload 不生效
- `deploy/gateway/nginx.conf` 是以**单文件** bind mount 进容器的。
- 改完宿主机文件后 `docker exec ... nginx -s reload` **完全没有生效**：宿主机 inode `1328492`，
  容器内 inode `1355595`，容器读的一直是启动时锁定的旧 inode。
- 根因：mini-novel 最近一次 rsync（15:20）**在网关容器启动（14:45）之后**替换了该文件，
  容器挂载就此失联。
- 修复：`docker restart mini-novel-gateway` 重新解析挂载。
- 处置前已 `diff` 确认容器内旧配置与磁盘打补丁前内容**完全一致**，故重启只引入本次新增块，
  不会改变 mini-novel 任何行为（重启中断约 1-2 秒）。

### ⚠️ 坑 2：`/opt/mini-h5` 是 mini-novel 的 rsync 同步目录，补丁会被覆盖
- 该目录每次 mini-novel 部署都会被覆盖 → 本次新增的 location 会**被抹掉**。
- 对策：补丁做成**幂等脚本** `ops/gateway-patch/apply-gateway-patch.sh`
  （服务器上 `/opt/video-entitlement-gateway/apply-gateway-patch.sh`），
  **mini-novel 每次重新部署后重跑一次即可恢复**。
- 根治建议（需用户决定）：把该 location 块**提交到 mini-novel 仓库上游**，或给网关容器
  额外挂一个 rsync 目录之外的 conf 文件。

### ⚠️ 坑 3：旧文档的 SSH key 已失效
- `PROJECT_MEMORY.md` / `问题一览.md` 都写 `~/.ssh/video-pro-key`，本机**该文件不存在**。
- 2026-09-15 实测可用的是 **`~/.ssh/mini_h5_vps_ed25519_v2`**（端口 52527，用户 root）。
- 已更新 `PROJECT_MEMORY.md`。

### ⚠️ 坑 4：本机 `github.com:443` 不通，但 `api.github.com` 通
- `github.com` 解析到 `20.205.243.166` → 连不上；而 `140.82.112.3` 等 IP 正常，
  `api.github.com:443` 完全可用（HTTP 200）。
- 影响：`git push` 走不通；但可用 **GitHub REST API**（api.github.com）提交改动并触发 workflow。

### ⚠️ 坑 5：本项目 nginx 未设 `client_max_body_size`（潜在 413）
- `video-entitlement-admin/docker/nginx.conf` 默认 1MB；管理后台上传 APK（约 7MB）时，
  若走 `http://IP:8082/admin/api/...` 直连入口，会在 nginx 层被 413 拦掉。
- 已补 `client_max_body_size 60m;`。（网关侧本身已有 `client_max_body_size 320m`，域名入口无此问题。）

---

## 四、改动清单

| 文件 | 改动 |
|------|------|
| `video-entitlement-admin/vite.config.ts` | 新增 `base: '/platform/'` |
| `video-entitlement-admin/src/main.tsx` | `BrowserRouter basename="/platform"` |
| `video-entitlement-admin/docker/nginx.conf` | 新增 `location /platform/` 前缀剥离 + `client_max_body_size 60m` |
| `video-entitlement-android/.../MainActivity.kt` | `API_BASE` → `https://xs2026.site` |
| `video-entitlement-android/.../LoginActivity.kt` | `API_BASE` → `https://xs2026.site` |
| `video-entitlement-android/build_and_deploy.sh` | `DOMAIN` → `https://xs2026.site`（脚本已弃用） |
| `.github/workflows/build-android.yml` | `DOMAIN` → `https://xs2026.site`（二维码/下载地址） |
| `.github/workflows/deploy.yml` | `APP_DOWNLOAD_BASE` → `https://xs2026.site/downloads`；新增 `docker network connect deploy_default` |
| `video-entitlement-server/.../application.yml` | `app.download-base` 默认值改新域名 |
| `.../AppVersionService.java` | `@Value` 默认值改新域名 |
| `.../AppVersionManage.tsx` | 下载地址示例文案改新域名 |
| `ops/gateway-patch/apply-gateway-patch.sh` | **新增**：网关接入幂等补丁脚本 |
| `PROJECT_MEMORY.md` / 本文件 | 文档沉淀 |

---

## 五、服务器侧已执行操作（全部为新增，未改 mini-novel 现有路由）

1. 备份原网关配置 → `/opt/mini-h5/deploy/gateway/nginx.conf.bak.20260915-153332`
2. 插入 video-entitlement 接入块（4 条 location）
3. `docker network connect deploy_default video-frontend` / `video-backend`
4. `nginx -t` 通过 → `docker restart mini-novel-gateway`（见坑 1）

## 六、验证结果（2026-09-15）

| 检查项 | 结果 |
|--------|------|
| `https://xs2026.site/platform/` | ✅ 200，入口 JS `/platform/assets/index-CwqQlcB6.js` 200（1.29MB） |
| `https://xs2026.site/api/v1/client/version` | ✅ 200 |
| `https://xs2026.site/admin/api/v1/versions` | ✅ 403（未鉴权，符合预期） |
| `https://xs2026.site/downloads/*.apk` / `qr-*.png` | ✅ 200 |
| IP 直连兜底 `:8082/`、`:8082/platform/`、`:8081/` | ✅ 全部 200 |
| mini-novel 回归 `/`、`/h5/home`、`/admin/login`、`/healthz`、`/api/home` | ✅ 全部 200，首页标题仍是 `Mini Novel` |
| 路由冲突 | ✅ 全仓 `grep api/v1` 仅命中本补丁块自身 |

---

## 七、CI 打包：中途踩的坑（Android）

### ⚠️ 坑 6：`android-actions/setup-android@v3` 已坏（上游变更）
第一次触发打包直接失败在 **Set up Android SDK** 步骤：
```
Warning: Failed to find package 'tools'
Error: The process '.../sdkmanager' failed with exit code 1
```
- 根因：该 action 内部会执行 `sdkmanager tools`，而**新版 Android SDK 仓库已移除 `tools` 包**。
  2026-09-01 那次还能跑通，属上游变更，与本项目代码无关。
- 修复：不再用该 action，改用 ubuntu-latest runner **预装的 Android SDK**，
  自动定位 cmdline-tools 并写入 PATH / ANDROID_HOME。

### ⚠️ 坑 7：`local.properties` 被跟踪，且写死了旧 action 的 SDK 路径
- 仓库里跟踪着 `video-entitlement-android/local.properties`，内容 `sdk.dir=/opt/android-sdk`
  —— 正是旧 action 的安装路径。
- **AGP 中 `local.properties` 的 `sdk.dir` 优先于 `ANDROID_HOME`**：换用 runner 预装 SDK
  （`/usr/local/lib/android/sdk`）后，若不覆写会直接报 `SDK location not found`。
- 修复：CI 里按探测到的真实 `SDK_ROOT` 覆写 `local.properties` 的 `sdk.dir`。

---

## 八、最终交付

### APK（本次重新打包）
| 项 | 值 |
|----|-----|
| versionName | `0.9.202609151543` |
| versionCode | `9151543` |
| 文件名 | `video-entitlement-v0.9.202609151543-20260915-154435.apk`（7.1MB） |
| 下载 | https://xs2026.site/downloads/video-entitlement-v0.9.202609151543-20260915-154435.apk |
| 最新版 | https://xs2026.site/downloads/video-entitlement-latest.apk |
| 二维码 | https://xs2026.site/downloads/qr-video-entitlement-v0.9.202609151543-20260915-154435.png |
| 构建 | [Build Android APK #34990170027](https://github.com/randy2014/mini-video-pro/actions/runs/34990170027) ✅ |

**APK 内容反查**（解 dex 后 grep）：`https://xs2026.site` 出现 1 次；
旧域名 `http://64.90.19.6:8081`、`64.90.19.6:8082` **0 次** → 确认打进去的就是新域名。

### 版本表登记
- 新版本 id=14 `0.9.202609151543` → **ACTIVE**
- 旧版本 id=13 `0.9.202609011425` → **INACTIVE**
- 登记脚本：`ops/deploy/register-version.sh`（服务器上 `/opt/video-entitlement-gateway/`）

---

## 九、GitHub 推送方式变更（本机限制）

⚠️ 本机 **`github.com:443` 不通**（DNS 解析到 `20.205.243.166`，被墙），
但 `api.github.com` 正常；且 hosts 文件无权限修改 → `git push` 无法使用。

对策：`ops/github/push-via-api.ps1` —— 走 **GitHub REST API（Git Data API）**
建 blob → tree → commit → 更新 `refs/heads/main`，等效于一次正常 push
（会正常触发 `deploy.yml`）。凭据取自 Windows 凭据管理器里已存的 GitHub 凭据，不落盘、不打印。

配套：
- `ops/github/files.txt` 待推送文件清单、`ops/github/message.txt` commit 信息（都存成 UTF-8 文件，
  避免 Windows PowerShell 5.1「UTF-8 当 ANSI 读」把脚本里的中文/引号搞坏）
- `ops/github/wait-runs.ps1` 轮询 workflow 运行状态

> ⚠️ 注意：本地 `main`（`c207854`）与远端 `main`（API 提交产生的 `a0538dac` / `4751f567`）
> 内容一致但 **commit SHA 不同**（API 建的提交本机没有对象）。下次推送前先核对 base SHA。

---

## 十、遗留 / 建议

1. **🔴 仓库当前是 public**（实测匿名 GET `api.github.com/repos/...` 返回 200、`private: false`），
   而 `README.md` 含明文 keystore 密码、`PROJECT_MEMORY.md` 含 DB/Redis/Admin 口令、
   仓库内还跟踪着 `mini-video-release.jks` 签名文件。建议立刻转私有并轮换密钥。
   （`问题一览.md` 第 9 条原以为"已转私有"，与实测不符。）
2. **mini-novel 重新部署会抹掉网关补丁** → 需重跑 `apply-gateway-patch.sh`。
   根治需把该 location 块提交到 mini-novel 仓库上游。
3. `deploy.yml` 末尾的 `docker system prune -a -f` 会清理**全机**未使用镜像（含 mini-novel 的构建缓存），
   虽不动运行中容器与数据卷，但与原则 #2「只操作本项目」略有张力，建议收窄为按镜像名前缀过滤。
4. 旧文档里的 SSH key 名（`~/.ssh/video-pro-key`）仍散落在 `问题一览.md`、`2026-07-19-工作沉淀.md` 等历史文档中，未逐一改写（属历史记录）。
